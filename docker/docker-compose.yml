version: '3.9'

services:
  ai-doc-generator:
    build:
      context: ..
      dockerfile: docker/Dockerfile

    container_name: ai-doc-generator

    ports:
      - '3001:3001'

    env_file:
      - ../.env

    restart: unless-stopped
